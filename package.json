{
  "name": "claude-adversary-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude-vs-Claude adversarial review plugin for Claude Code.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
